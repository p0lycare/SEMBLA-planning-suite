# Vertraulich – NICHT ins (öffentliche) Repo
Uploads/
# Reale Bauteilgeometrie (nur lokal; Tools laden sie zur Laufzeit per Upload)
Bauteil-OBJ/

# Abhängigkeiten (regenerierbar via `npm install`)
node_modules/

# macOS / OS-Müll
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor/Temp
*.log
/tmp/
# Word-Sperrdateien (entstehen, solange doku/*.docx geoeffnet ist)
~$*
# Lokale Debug-/Seed-Helfer in docs/ (Unterstrich-Praefix, gehoeren nicht auf GH Pages)
docs/_*.html

# Python-Cache (regenerierbar)
__pycache__/
*.pyc
